{
  "name": "uxa",
  "private": true,
  "version": "0.0.2",
  "devDependencies": {
    "imba-router": "^0.2.0",
    "less-plugin-clean-css": "^1.5.1"
  },
  "main": "src/index",
  "scripts": {
    "build": "imbapack --watch --colors --display-modules"
  },
  "dependencies": {
    "marked": "^0.3.6"
  },
  "peerDependencies": {
    "imba": "1.x"
  }
}
